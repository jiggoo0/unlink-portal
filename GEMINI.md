# 🔒 LOCAL AI MANDATE: UNLINK ECOSYSTEM NODE
**Inheritance:** This project inherits all rules, styling, and architectural mandates from `~/projects/GEMINI.md`.
**Global Authority:** DO NOT make architectural or design decisions without consulting the global mandate first.

## 🛠️ LOCAL DEVELOPMENT RULES
1. **Theme:** Must strictly adhere to the "Institutional Authority" aesthetic (Light, Professional, Slate/Blue).
2. **Commands:** ONLY use `pnpm` for dependency management.
3. **Build:** MUST use `next build --webpack` to ensure Termux WASM compatibility.
4. **Data:** Must connect to the Shared Supabase defined in `~/projects/Note.md`.
5. **Security:** NEVER commit `.env` files to version control.
